from setuptools import setup

setup(name         = 'package_name',
      version      = '0.0.1',
      description  = '',
      url          = 'http://github.com/rimbaudcode/package_name',
      author       = 'rimbaudcode',
      author_email = 'rimbaudcode[aT]gmail(DoT)com',
      license      = 'BSD3',
      packages     = ['package_name'],
      zip_safe     = False)
